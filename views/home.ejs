<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>URL Shortener</title>
    <style>
      /* Reset some default styles */
      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Franklin Gothic Medium", "Arial Narrow", Arial, sans-serif;
        background-color: #192734; /* dark blue-gray background */
        color: #e0e7ff; /* soft light text */
        margin: 0;
        padding: 40px 20px;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        min-height: 100vh;
      }

      .container {
        background-color: #223449; /* slightly lighter than BG */
        padding: 30px 40px;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
        max-width: 450px;
        width: 100%;
        text-align: center;
      }

      h1 {
        margin-bottom: 24px;
        font-size: 2.4rem;
        color: #58a6ff; /* bright blue accent */
        text-shadow: 0 1px 6px rgba(88, 166, 255, 0.6);
      }

      form label {
        display: block;
        font-weight: 600;
        margin-bottom: 8px;
        text-align: left;
        color: #a6c1ff;
        font-size: 1rem;
      }

      input[type="text"] {
        width: 100%;
        padding: 12px 16px;
        font-size: 1rem;
        border-radius: 8px;
        border: 1.5px solid #4a90e2;
        background-color: #1b2a3c;
        color: #e0e7ff;
        transition: border-color 0.3s ease;
        outline: none;
      }
      input[type="text"]::placeholder {
        color: #7a95bf;
      }
      input[type="text"]:focus {
        border-color: #58a6ff;
        box-shadow: 0 0 8px #4d90f6aa;
      }

      button[type="submit"] {
        margin-top: 20px;
        width: 100%;
        padding: 12px 0;
        font-size: 1.1rem;
        font-weight: 700;
        color: #fff;
        background: linear-gradient(135deg, #58a6ff, #1c3c72);
        border: none;
        border-radius: 10px;
        cursor: pointer;
        transition: background 0.3s ease, box-shadow 0.3s ease;
      }
      button[type="submit"]:hover {
        background: linear-gradient(135deg, #1c3c72, #58a6ff);
        box-shadow: 0 0 10px #79b3ffcc;
      }

      /* Generated URL display */
      .generated-url {
        margin-top: 30px;
        font-weight: 700;
        font-size: 1.1rem;
        color: #cde0ff;
        word-break: break-all;
      }
      .generated-url a {
        color: #81a8ff;
        text-decoration: none;
        border-bottom: 2px solid transparent;
        transition: border-color 0.3s ease;
      }
      .generated-url a:hover {
        border-color: #81a8ff;
      }

      /* Responsive adjustments */
      @media (max-width: 480px) {
        .container {
          padding: 25px 20px;
          max-width: 100%;
        }

        h1 {
          font-size: 2rem;
        }

        button[type="submit"] {
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>URL Shortener</h1>
      <form method="POST" action="/URL">
        <label for="url-input">Enter your Original URL</label>
        <input
          id="url-input"
          type="text"
          name="url"
          placeholder="https://example.com"
          required
          autocomplete="off"
        />
        <button type="submit">Generate</button>
      </form>

      <% if(locals.id) { %>
      <p class="generated-url">
        URL Generated:
        <a
          href="http://localhost:3001/URL/<%= id %>"
          target="_blank"
          rel="noopener noreferrer"
        >
          http://localhost:3001/URL/<%= id %>
        </a>
      </p>
      <% } %>
    </div>
  </body>
</html>
